# NPM Supply Chain Monitor Environment Configuration
# Copy this file to .env and customize the values

# Slack Configuration (Required)
SLACK_WEBHOOK_URL=<hook>

# Host Identification (Optional)
HOSTNAME=npm-monitor-prod
RUNTIME_USER=monitor-service

# Monitor Configuration (Optional - these can be modified in npm_scanner.py)
# SCAN_INTERVAL_MINUTES=15
# MIN_PACKAGES_THRESHOLD=5
# MIN_DIVERSITY_SCORE=0.8

# Database Configuration
# DB_PATH=/data/npm_scanner.db

# Logging Level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
